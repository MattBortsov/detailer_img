# Copy this file to .env on the server and fill in the real values.
# Do not commit .env or use production secrets in CI.

TELEGRAM_BOT_TOKEN=
OPENROUTER_API_KEY=
OPENROUTER_IMAGE_MODEL=x-ai/grok-imagine-image-quality

# Runtime settings will be added when the bot and Mini App are implemented.
